{
  "name": "skidip",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@algolia/fragments.js": "^2.0.0-beta.12",
    "@algolia/ui-library": "^1.0.0-beta.91",
    "@emotion/core": "^10.0.10",
    "chroma-js": "^2.0.4",
    "d3-dsv": "^1.1.1",
    "emotion": "^10.0.9",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": " yarn build && git add . && git commit -m'update' && git push origin master && git subtree push  --prefix build origin gh-pages"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
